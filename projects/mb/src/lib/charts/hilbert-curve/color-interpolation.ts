/**
 * Given a number t in the range [0,1], returns the corresponding color represented as an RGB string.
 *
 * @param t Number in the range [0, 1].
 */
export type ColorInterpolation = (t: number) => string;
